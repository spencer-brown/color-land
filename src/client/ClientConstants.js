const ClientConstants = {
  MenuConstants: {
    HOME: 'HOME',
    IN_GAME: 'IN_GAME'
  }
};

// Exported non-ES6 so that we can import certain properties and subobjects of the object.
module.exports = ClientConstants; 